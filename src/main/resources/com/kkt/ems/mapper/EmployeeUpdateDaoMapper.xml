<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkt.ems.mapper.EmployeeUpdateDaoMapper">
	<select id="updater" parameterType="ShainInfoDto">

		UPDATE
		SHAIN_INFO
		SET
		SHAIN_NAME_KJ = #{shainNameKj} ,
		SHAIN_NAME_KN = #{shainNameKn} ,
		BUSHO_CD = #{bushoName}
		WHERE
		SHAIN_NO = #{shainNo}

	</select>
</mapper>